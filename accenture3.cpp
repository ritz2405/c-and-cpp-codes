#include<bits/stdc++.h>
using namespace std;
int main()
{
	string s;
	cin>>s;
	if(s.length()<4)
	{
		cout<<0<<"\n";
	}
	if(s[0]>=0 || s[0] <=9)
	{
		cout<<0<<"\n";
	}
	int i=0;
	while(s[i]!='\0')
	{
		if(s[i] >= 65 && s[i] <= 90)
		{
			
		}
	}
}